/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Kargo API
 * REST API for Kargo
 * OpenAPI spec version: v1alpha1
 */

export interface PromotionScheduleStatus {
  /** Closed indicates that the schedule currently forbids promotion of this
Stage.

+optional */
  closed?: boolean;
  /** NextClose is when the schedule is next expected to forbid promotions, and
is meaningful only while Closed is false. It allows a client to give warning
of an approaching freeze. Like NextOpen it is optional, and absent when no
such boundary is known.

+optional */
  nextClose?: string;
  /** NextOpen is when the schedule is next expected to permit promotions.

It is optional even while Closed is true, and its absence means only that
no reopening is known: the schedule may have none (a one-shot Allow window
that has already elapsed), determining one may be impractical, or the
closure may be indefinite by design. Clients must therefore render Reason
and treat a missing NextOpen as "frozen, with no known end" rather than
assuming a value is present.

+optional */
  nextOpen?: string;
  /** Reason explains in human-readable terms why the schedule is closed, naming
the window responsible where one is. It is always set while Closed is true,
and is the only field guaranteed to explain a freeze -- NextOpen may be
absent. It carries the same explanation as the corresponding admission
rejection.

+optional */
  reason?: string;
}
